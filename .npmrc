ignore-scripts=true
audit=true
# Don't install any version published less than a day ago. A poisoned release
# is usually detected and yanked within hours, so even a short cooldown filters
# most compromises. Needs npm >= 11.10.0 (older npm silently ignores it).
min-release-age=1
